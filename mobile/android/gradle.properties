# Project-wide Gradle settings.

# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
org.gradle.parallel=true
org.gradle.caching=true

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true

# ---------- OneDrive / Cloud-Synced Folder Build Fixes ----------
# Problem: "Cannot snapshot assets" error occurs because OneDrive actively locks
# and re-syncs temp files that Gradle / aapt2 creates for incremental builds.

# 1. Disable aapt2 resource snapshots (the direct cause of the "Cannot snapshot" error)
android.enableAapt2WorkspaceSnapshots=false

# 2. Disable VCS (file watching) snapshots for file-systems backed by cloud sync
android.gradle.workspace.snapshots.enabled=false

# 3. Move Gradle build cache OUTSIDE the OneDrive-synced project folder
#    (Uses per-user home dir which should NOT be in OneDrive)
org.gradle.project.build.dir=../../../../../.android-build-cache/foodscan-ai/build
systemProp.gradle.user.home=${user.home}/.gradle-foodscan

# 4. Keep dex archives in non-synced temp for speed
android.enableDexingArtifactArchive=false

# 5. Non-transitive R classes (saves build time + avoids conflicts)
android.nonTransitiveRClass=true
